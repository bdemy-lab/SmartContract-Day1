{
  "name": "smart-contracts",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "prettier": "prettier --write **/*.sol",
    "compile": "hardhat compile",
    "sign": "hardhat run scripts/sign.js --network testnet"

  },
  "dependencies": {
    "config-js": "^1.1.15",
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "@openzeppelin/contracts": "^4.7.0",
    "ethers": "^5.6.9",
    "hardhat": "^2.9.9"
  }
}
